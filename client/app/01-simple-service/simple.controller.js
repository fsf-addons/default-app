(function () {
    angular
        .module("sample")
        .controller("SimpleCtrl", [SimpleCtrl]);

    // 01. Inject Service

    function SimpleCtrl() {
        // 02. Expect service

        // 03. Access the variable

        // 04. Access the method in service
    }
})();